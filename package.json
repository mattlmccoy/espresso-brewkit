{
  "name": "espresso-brewkit",
  "private": true,
  "type": "module",
  "description": "Measurement tools for espresso. The site itself has no build step and no runtime dependencies; Playwright is used only to test it.",
  "scripts": {
    "test": "npm run --silent check && node test/ui.test.mjs",
    "serve": "node -e \"import('./test/server.mjs').then(m=>m.serve()).then(s=>console.log(s.origin))\"",
    "check": "node --check test/ui.test.mjs && node --check test/server.mjs && node --check bin/dev.mjs && node -e \"import('./bin/checksite.mjs')\"",
    "start": "node bin/dev.mjs",
    "start:lan": "node bin/dev.mjs --lan"
  },
  "devDependencies": {
    "playwright": "^1.56.0"
  }
}
